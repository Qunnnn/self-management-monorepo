name: self_management_mcp_server
description: MCP server exposing the Self-Management backend API as tools and resources.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  dart_mcp: ^0.1.0
  dio: ^5.9.0
